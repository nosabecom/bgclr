{
  "name": "nosa-c-reference",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "node --test scripts/search.test.mjs",
    "index:algolia": "node --env-file-if-exists=.env.local scripts/index-algolia.mjs"
  },
  "dependencies": {
    "algoliasearch": "^5.57.0",
    "minisearch": "^7.2.0"
  },
  "devDependencies": {
    "cheerio": "^1.2.0",
    "esbuild": "^0.28.2"
  }
}
